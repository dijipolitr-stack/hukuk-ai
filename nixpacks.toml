[phases.build]
cmds = ["pip install -r requirements.txt"]

[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[start]
cmd = "cd petition_engine && uvicorn api:app --host 0.0.0.0 --port "
